# Private working notes — not for the public repo
.private/

# Local tooling state (scheduled tasks lock, launch configs, etc.)
.claude/

# Swift / Xcode
.build/
.swiftpm/
*.xcodeproj/xcuserdata/
DerivedData/
.DS_Store
*.moved-aside

# SwiftPM resolved deps are fine to track for an app but this is a library; keep untracked during early dev
Package.resolved
